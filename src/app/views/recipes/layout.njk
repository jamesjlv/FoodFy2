<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0"> {% block head %}{% endblock %}
        {% include '../parts/styles.njk' %}
        <title>
            {% block pageName %}{% endblock %}
        </title>
    </head>
    <body>
        <header>
            <nav>
                <div class="logo">
                    <a href="/"><img src="/images/logo.png" alt="Logotipo FoodFy">
                    </a>
                </div>
                <div class="headOptionsAndFilter">
                    <div class="links">
                        <a href="/about">Sobre</a>
                        <a href="/recipes">Receitas</a>
                        <a href="/recipes/chefs">Chefs</a>
                    </div>
                    {% if chefs %}
                        {% else %}
                            <div class="filter">
                                <form action="/recipes" method="get">
                                    <input type="text" name="filter" id="filter" value="{{ filter }}" placeholder="Procure por uma receita">
                                    <button type="submit">Buscar</button>
                                </form>
                            </div>
                    {% endif %}
                </div>
            </nav>
        </header>
        <main>
            {% block content %}{% endblock %}
        </main>
        <footer>
            <span>
                <p>Todos os direitos reservados, receitas FoodFy</p>
            </span>
        </footer>
        <script src="/recipesEvent.js"></script>
        {% block scripts %}{% endblock %}
    </body>
</html>